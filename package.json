{
  "name": "pcg-grading-tracker",
  "version": "1.1.0",
  "private": true,
  "description": "Look up a PCG Grading certificate and save the highest-resolution photos it exposes. Runs with no dependencies; optionally uses a real browser.",
  "main": "pcg.js",
  "bin": { "pcg": "pcg.js" },
  "scripts": {
    "start": "node pcg.js",
    "mock": "node test/mock-server.js 8799",
    "test": "node test/run-test.js"
  },
  "optionalDependencies": {
    "playwright": "^1.49.0"
  },
  "engines": { "node": ">=18" }
}
